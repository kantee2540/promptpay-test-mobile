name: prompypay_qr_test
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  crclib: ^3.0.0
  flutter:
    sdk: flutter
  qr_flutter: ^4.1.0
  string_to_hex: ^0.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
